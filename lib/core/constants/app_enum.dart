enum YesNo { yes, no }

enum UserType {
  superAdmin,
  organizationAdmin,
  manager,
  departmentHead,
  employee,
}

enum APIMethods {
  get,
  post,
  put,
  update,
  delete,
  patch,
}

enum LoggerType { debug, info, warning, error }
