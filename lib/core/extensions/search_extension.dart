// import 'package:up_down_app/models/workout_model.dart';

// extension SearchListExtension on List<WorkOutModel> {
//   bool containsSearch(String searchTerm) {
//     return any((workout) => workout.name.toLowerCase().contains(searchTerm.toLowerCase()));
//   }
// }
